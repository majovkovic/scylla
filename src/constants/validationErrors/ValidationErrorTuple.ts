export const VALIDATION_ERROR_TUPLE_MESSAGES = {
  IS_TUPLE: (length: number) => `Value must be a tuple of length ${length}.`,
  TUPLE_ELEMENT_TYPE: 'Tuple element types must match specified types.',
};
